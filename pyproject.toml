[project]
name = "ComfyUI-Qwen3.5-Uncensored-GGUF"
description = "GGUF-only build of Qwen3.5-Uncensored for ComfyUI. Uses llama-cpp-python instead of transformers — no transformers dependency, compatible with older ComfyUI nodes."
version = "1.0.0"
license = {file = "LICENSE"}
dependencies = ["torch", "huggingface-hub", "hf_xet", "numpy", "Pillow", "llama-cpp-python"]

[project.urls]
Repository = "https://github.com/Deaquay/ComfyUI-Qwen3.5-Uncensored-GGUF"

[tool.comfy]
PublisherId = "Deaquay"
DisplayName = "ComfyUI-Qwen3.5-Uncensored-GGUF"
Icon = ""
Category = "video"
